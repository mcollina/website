---
title: Blog
in_menu: true
sort_info: 20
--- pipeline:translator,erb,tags,maruku

<% 
  posts = context.website.tree.node_access[:path].values.select do |n|
    Webgen::Path.match(n.path,"/blog/**/*.html") and n.lang == context.node.lang
  end
  
  posts.sort! do |a,b|
    a.meta_info["created_at"] <=> b.meta_info["created_at"]
  end

  template = context.website.tree.node_access[:path]["/blog/default.template"]
%>

<h1><%= posts.first["title"] %></h1>

<%= posts.first.node_info[:page].blocks["content"].content %>

<h1>{t:blog.oldpost.title}</h1>

<p>
{t:blog.oldpost.content}
<ul>
<% posts.each do |post| %>
<li>
  <%= context.node.link_to(post) %>
</li>
<% end %>
</ul>

</p>


